{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend/chat-app && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "private": true
}
